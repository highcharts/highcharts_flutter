# Scrollable chart demo

**Highcharts demo**, **Scrollable column & spline chart**
Scrollable chart showing multiple data series with columns and a line.
Demonstrates the use of the `scrollablePlotArea` feature for handling large datasets.

# How to run

In the demo directory:

```
flutter create .
flutter pub get
flutter run
```

