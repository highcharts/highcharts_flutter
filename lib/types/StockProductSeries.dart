/**
 * Highcharts Flutter Integration
 * 
 * Copyright (c), Highsoft AS 2023
 * 
 * sales@highcharts.com
 * support@highcharts.com
 * 
 * The use of this software requires a valid license.
 * 
 * See https://highcharts.com/license
 * 
 *
 * Built for Highcharts v.xx.
 * Build stamp: 2024-03-22
 *
 */ 

import 'ABandsOptions.dart';
import 'ADOptions.dart';
import 'AOOptions.dart';
import 'APOOptions.dart';
import 'AreaSeriesOptions.dart';
import 'AreaRangeSeriesOptions.dart';
import 'AreaSplineSeriesOptions.dart';
import 'AreaSplineRangeSeriesOptions.dart';
import 'AroonOptions.dart';
import 'AroonOscillatorOptions.dart';
import 'ATROptions.dart';
import 'BBOptions.dart';
import 'BubbleSeriesOptions.dart';
import 'CandlestickSeriesOptions.dart';
import 'CCIOptions.dart';
import 'ChaikinOptions.dart';
import 'CMFOptions.dart';
import 'CMOOptions.dart';
import 'ColumnSeriesOptions.dart';
import 'PyramidSeriesOptions.dart';
import 'ColumnRangeSeriesOptions.dart';
import 'DEMAOptions.dart';
import 'DisparityIndexOptions.dart';
import 'DMIOptions.dart';
import 'DPOOptions.dart';
import 'DumbbellSeriesOptions.dart';
import 'EMAOptions.dart';
import 'FlagsSeriesOptions.dart';
import 'HeikinAshiSeriesOptions.dart';
import 'HLCSeriesOptions.dart';
import 'HollowCandlestickSeriesOptions.dart';
import 'IKHOptions.dart';
import 'KeltnerChannelsOptions.dart';
import 'KlingerOptions.dart';
import 'LineSeriesOptions.dart';
import 'LinearRegressionOptions.dart';
import 'LinearRegressionAngleOptions.dart';
import 'LinearRegressionInterceptOptions.dart';
import 'LinearRegressionSlopesOptions.dart';
import 'LollipopSeriesOptions.dart';
import 'MACDOptions.dart';
import 'MFIOptions.dart';
import 'MomentumOptions.dart';
import 'NATROptions.dart';
import 'OBVOptions.dart';
import 'OHLCSeriesOptions.dart';
import 'PCOptions.dart';
import 'PivotPointsOptions.dart';
import 'PolygonSeriesOptions.dart';
import 'PPOOptions.dart';
import 'PriceEnvelopesOptions.dart';
import 'PSAROptions.dart';
import 'ROCOptions.dart';
import 'RSIOptions.dart';
import 'ScatterSeriesOptions.dart';
import 'SlowStochasticOptions.dart';
import 'SMAOptions.dart';
import 'SplineSeriesOptions.dart';
import 'StochasticOptions.dart';
import 'StreamgraphSeriesOptions.dart';
import 'SupertrendOptions.dart';
import 'TEMAOptions.dart';
import 'TrendLineOptions.dart';
import 'TRIXOptions.dart';
import 'VBPOptions.dart';
import 'VectorSeriesOptions.dart';
import 'VWAPOptions.dart';
import 'WilliamsROptions.dart';
import 'WindbarbSeriesOptions.dart';
import 'WMAOptions.dart';
import 'XRangeSeriesOptions.dart';
import 'ZigzagOptions.dart';
import 'OptionFragment.dart';

/** 
 * StockProductSeries 
 */
class StockProductSeries extends OptionFragment {
  StockProductSeries() : super();
  

  //////////////////////////////////////////////////////////////////////////////
  
  @override
  void toJSONInner(StringBuffer buffer) {
    super.toJSONInner(buffer);

    
    // NOTE: skip serialization of abands (type ABandsOptions is ignored)} 

    // NOTE: skip serialization of ad (type ADOptions is ignored)} 

    // NOTE: skip serialization of ao (type AOOptions is ignored)} 

    // NOTE: skip serialization of apo (type APOOptions is ignored)} 

    // NOTE: skip serialization of area (type AreaSeriesOptions is ignored)} 

    // NOTE: skip serialization of arearange (type AreaRangeSeriesOptions is ignored)} 

    // NOTE: skip serialization of areaspline (type AreaSplineSeriesOptions is ignored)} 

    // NOTE: skip serialization of areasplinerange (type AreaSplineRangeSeriesOptions is ignored)} 

    // NOTE: skip serialization of aroon (type AroonOptions is ignored)} 

    // NOTE: skip serialization of aroonoscillator (type AroonOscillatorOptions is ignored)} 

    // NOTE: skip serialization of atr (type ATROptions is ignored)} 

    // NOTE: skip serialization of bb (type BBOptions is ignored)} 

    // NOTE: skip serialization of bubble (type BubbleSeriesOptions is ignored)} 

    // NOTE: skip serialization of candlestick (type CandlestickSeriesOptions is ignored)} 

    // NOTE: skip serialization of cci (type CCIOptions is ignored)} 

    // NOTE: skip serialization of chaikin (type ChaikinOptions is ignored)} 

    // NOTE: skip serialization of cmf (type CMFOptions is ignored)} 

    // NOTE: skip serialization of cmo (type CMOOptions is ignored)} 

    // NOTE: skip serialization of column (type ColumnSeriesOptions is ignored)} 

    // NOTE: skip serialization of columnpyramid (type PyramidSeriesOptions is ignored)} 

    // NOTE: skip serialization of columnrange (type ColumnRangeSeriesOptions is ignored)} 

    // NOTE: skip serialization of dema (type DEMAOptions is ignored)} 

    // NOTE: skip serialization of disparityindex (type DisparityIndexOptions is ignored)} 

    // NOTE: skip serialization of dmi (type DMIOptions is ignored)} 

    // NOTE: skip serialization of dpo (type DPOOptions is ignored)} 

    // NOTE: skip serialization of dumbbell (type DumbbellSeriesOptions is ignored)} 

    // NOTE: skip serialization of ema (type EMAOptions is ignored)} 

    // NOTE: skip serialization of flags (type FlagsSeriesOptions is ignored)} 

    // NOTE: skip serialization of heikinashi (type HeikinAshiSeriesOptions is ignored)} 

    // NOTE: skip serialization of hlc (type HLCSeriesOptions is ignored)} 

    // NOTE: skip serialization of hollowcandlestick (type HollowCandlestickSeriesOptions is ignored)} 

    // NOTE: skip serialization of ikh (type IKHOptions is ignored)} 

    // NOTE: skip serialization of keltnerchannels (type KeltnerChannelsOptions is ignored)} 

    // NOTE: skip serialization of klinger (type KlingerOptions is ignored)} 

    // NOTE: skip serialization of line (type LineSeriesOptions is ignored)} 

    // NOTE: skip serialization of linearregression (type LinearRegressionOptions is ignored)} 

    // NOTE: skip serialization of linearregressionangle (type LinearRegressionAngleOptions is ignored)} 

    // NOTE: skip serialization of linearregressionintercept (type LinearRegressionInterceptOptions is ignored)} 

    // NOTE: skip serialization of linearregressionslope (type LinearRegressionSlopesOptions is ignored)} 

    // NOTE: skip serialization of lollipop (type LollipopSeriesOptions is ignored)} 

    // NOTE: skip serialization of macd (type MACDOptions is ignored)} 

    // NOTE: skip serialization of mfi (type MFIOptions is ignored)} 

    // NOTE: skip serialization of momentum (type MomentumOptions is ignored)} 

    // NOTE: skip serialization of natr (type NATROptions is ignored)} 

    // NOTE: skip serialization of obv (type OBVOptions is ignored)} 

    // NOTE: skip serialization of ohlc (type OHLCSeriesOptions is ignored)} 

    // NOTE: skip serialization of pc (type PCOptions is ignored)} 

    // NOTE: skip serialization of pivotpoints (type PivotPointsOptions is ignored)} 

    // NOTE: skip serialization of polygon (type PolygonSeriesOptions is ignored)} 

    // NOTE: skip serialization of ppo (type PPOOptions is ignored)} 

    // NOTE: skip serialization of priceenvelopes (type PriceEnvelopesOptions is ignored)} 

    // NOTE: skip serialization of psar (type PSAROptions is ignored)} 

    // NOTE: skip serialization of roc (type ROCOptions is ignored)} 

    // NOTE: skip serialization of rsi (type RSIOptions is ignored)} 

    // NOTE: skip serialization of scatter (type ScatterSeriesOptions is ignored)} 

    // NOTE: skip serialization of slowstochastic (type SlowStochasticOptions is ignored)} 

    // NOTE: skip serialization of sma (type SMAOptions is ignored)} 

    // NOTE: skip serialization of spline (type SplineSeriesOptions is ignored)} 

    // NOTE: skip serialization of stochastic (type StochasticOptions is ignored)} 

    // NOTE: skip serialization of streamgraph (type StreamgraphSeriesOptions is ignored)} 

    // NOTE: skip serialization of supertrend (type SupertrendOptions is ignored)} 

    // NOTE: skip serialization of tema (type TEMAOptions is ignored)} 

    // NOTE: skip serialization of trendline (type TrendLineOptions is ignored)} 

    // NOTE: skip serialization of trix (type TRIXOptions is ignored)} 

    // NOTE: skip serialization of vbp (type VBPOptions is ignored)} 

    // NOTE: skip serialization of vector (type VectorSeriesOptions is ignored)} 

    // NOTE: skip serialization of vwap (type VWAPOptions is ignored)} 

    // NOTE: skip serialization of williamsr (type WilliamsROptions is ignored)} 

    // NOTE: skip serialization of windbarb (type WindbarbSeriesOptions is ignored)} 

    // NOTE: skip serialization of wma (type WMAOptions is ignored)} 

    // NOTE: skip serialization of xrange (type XRangeSeriesOptions is ignored)} 

    // NOTE: skip serialization of zigzag (type ZigzagOptions is ignored)} 
  }

}
