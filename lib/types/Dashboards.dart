/**
 * Highcharts Flutter Integration
 * 
 * Copyright (c), Highsoft AS 2023
 * 
 * sales@highcharts.com
 * support@highcharts.com
 * 
 * The use of this software requires a valid license.
 * 
 * See https://highcharts.com/license
 * 
 *
 * Built for Highcharts v.xx.
 * Build stamp: 2024-04-03
 *
 */ 

import 'OptionFragment.dart';

/** 
 * Dashboards 
 */
class Dashboards extends OptionFragment {
  Dashboards() : super();
  

  //////////////////////////////////////////////////////////////////////////////
  
  @override
  void toJSONInner(StringBuffer buffer) {
    super.toJSONInner(buffer);

    
    // NOTE: skip serialization of boards (type Board[] is ignored)} 

    // NOTE: skip serialization of win (type Window & typeof globalThis is ignored)} 

    // NOTE: skip serialization of AST (type typeof AST is ignored)} 

    // NOTE: skip serialization of Board (type typeof Board is ignored)} 

    // NOTE: skip serialization of Component (type typeof Component is ignored)} 

    // NOTE: skip serialization of ComponentRegistry (type typeof ComponentRegistry is ignored)} 

    // NOTE: skip serialization of DataConnector (type typeof DataConnector is ignored)} 

    // NOTE: skip serialization of DataCursor (type typeof DataCursor is ignored)} 

    // NOTE: skip serialization of DataModifier (type typeof DataModifier is ignored)} 

    // NOTE: skip serialization of DataPool (type typeof DataPool is ignored)} 

    // NOTE: skip serialization of DataTable (type typeof DataTable is ignored)} 

    // NOTE: skip serialization of DataGridPlugin (type Generic is ignored)} 

    // NOTE: skip serialization of HighchartsPlugin (type Generic is ignored)} 

    // NOTE: skip serialization of PluginHandler (type typeof PluginHandler is ignored)} 

    // NOTE: skip serialization of Sync (type typeof Sync is ignored)} 

    // NOTE: skip serialization of EditMode (type typeof EditMode is ignored)} 

    // NOTE: skip serialization of FullScreen (type typeof Fullscreen is ignored)} 

    // NOTE: skip serialization of Formula (type { isFormula: (item: Item) => item is Formula; isFunction: (item: Item) => item is Function; isOperator: (item: Item) => item is Operator; isRange: (item: Item) => item is Range; ... 11 more ...; parseFormula: (text: string, alternativeSeparators: boolean) => Formula; } is ignored)} 
  }

}
