/**
 * Highcharts Flutter Integration
 * 
 * Copyright (c), Highsoft AS 2023
 * 
 * sales@highcharts.com
 * support@highcharts.com
 * 
 * The use of this software requires a valid license.
 * 
 * See https://highcharts.com/license
 * 
 *
 * Built for Highcharts v.xx.
 * Build stamp: 2024-04-18
 *
 */ 

import 'MapNavigationOptions.dart';
import 'MapViewOptions.dart';
import 'ChartOptions.dart';
import 'LegendOptions.dart';
import 'TooltipOptions.dart';
import 'DataClassOptions.dart';
import 'GradientColorStop.dart';
import 'MarkerOptions.dart';
import 'LabelsOptions.dart';
import 'RangesOptions.dart';
import 'RangeSelectorOptions.dart';
import 'LangOptions.dart';
import 'LoadingOptions.dart';
import 'GlobalOptions.dart';
import 'CaptionOptions.dart';
import 'CreditsOptions.dart';
import 'SubtitleOptions.dart';
import 'SeriesOptions.dart';
import 'TitleOptions.dart';
import 'TimeOptions.dart';
import 'AccessibilityOptions.dart';
import 'ExportingOptions.dart';
import 'NavigationOptions.dart';
import 'AnnotationOptions.dart';
import 'NavigatorOptions.dart';
import 'ScrollbarOptions.dart';
import 'PaneOptions.dart';
import 'ConnectorsOptions.dart';
import 'FrameOptions.dart';
import 'Options.dart';
import 'RuleOptions.dart';
import 'CSSJSONObject.dart';
import 'JSON.dart';
import 'SnapOptions.dart';
import 'ElementStyles.dart';
import 'CloseIcon.dart';
import 'SettingsOptions.dart';
import 'Toolbars.dart';
import 'Tools.dart';
import 'NestedOptions.dart';
import 'SelectOptions.dart';
import 'ColumnNamesOptions.dart';
import 'OptionsBindings.dart';
import 'ComponentConnectorOptions.dart';
import 'Node.dart';
import 'DataPoolOptions.dart';
import 'GUIOptions.dart';
import 'ResponsiveBreakpoints.dart';
import 'DataGridOptions.dart';
import 'SyncOptions.dart';
import 'ConnectorOptions.dart';
import 'CSSObject.dart';
import 'LinkedValueToOptions.dart';
import 'GoogleSpreadsheetJSON.dart';
import 'HTMLElement.dart';
import 'TypeOptions.dart';
import 'LabelOptions.dart';
import 'BoostOptions.dart';
import 'DataOptions.dart';
import 'DrilldownOptions.dart';
import 'NoDataOptions.dart';
import 'BubblePointMarkerOptions.dart';
import 'StockToolsOptions.dart';
import 'OptionFragment.dart';

/** 
 * Options 
 */
class Options extends OptionFragment {
  Options( {
    this.approximation = null,
    this.enableSimulation = null,
    this.friction = null,
    this.gravitationalConstant = null,
    this.initialPositionRadius = null,
    this.integration = null,
    this.linkLength = null,
    this.maxIterations = null,
    this.maxSpeed = null,
    this.theta = null,
    this.type = null
  }) : super();
  // NOTE: dataClassColor skipped - type string is ignored in gen 

  // NOTE: maxColor skipped - type string is ignored in gen 

  // NOTE: minColor skipped - type string is ignored in gen 

  // NOTE: layout skipped - type string is ignored in gen 

  // NOTE: showInLegend skipped - type boolean is ignored in gen 

  // NOTE: borderColor skipped - type string is ignored in gen 

  // NOTE: borderWidth skipped - type number is ignored in gen 

  // NOTE: className skipped - type string is ignored in gen 

  // NOTE: color skipped - type string is ignored in gen 

  // NOTE: connectorClassName skipped - type string is ignored in gen 

  // NOTE: connectorColor skipped - type string is ignored in gen 

  // NOTE: connectorDistance skipped - type number is ignored in gen 

  // NOTE: connectorWidth skipped - type number is ignored in gen 

  // NOTE: enabled skipped - type boolean is ignored in gen 

  // NOTE: legendIndex skipped - type number is ignored in gen 

  // NOTE: maxSize skipped - type number is ignored in gen 

  // NOTE: minSize skipped - type number is ignored in gen 

  // NOTE: placed skipped - type boolean is ignored in gen 

  // NOTE: seriesIndex skipped - type number is ignored in gen 

  // NOTE: sizeBy skipped - type string is ignored in gen 

  // NOTE: sizeByAbsoluteValue skipped - type boolean is ignored in gen 

  // NOTE: zIndex skipped - type number is ignored in gen 

  // NOTE: zThreshold skipped - type number is ignored in gen 

  // NOTE: exposeElementToA11y skipped - type boolean is ignored in gen 

  // NOTE: alpha skipped - type number is ignored in gen 

  // NOTE: beta skipped - type number is ignored in gen 

  // NOTE: depth skipped - type number is ignored in gen 

  // NOTE: fitToPlot skipped - type boolean is ignored in gen 

  // NOTE: viewDistance skipped - type number is ignored in gen 

  // NOTE: isGantt skipped - type boolean is ignored in gen 

  // NOTE: isStock skipped - type boolean is ignored in gen 

  // NOTE: isResponsiveOptions skipped - type boolean is ignored in gen 

  // NOTE: id skipped - type string is ignored in gen 

  // NOTE: parentContainerId skipped - type string is ignored in gen 

  // NOTE: copyId skipped - type string is ignored in gen 

  // NOTE: layoutClassName skipped - type string is ignored in gen 

  // NOTE: rowClassName skipped - type string is ignored in gen 

  // NOTE: cellClassName skipped - type string is ignored in gen 

  // NOTE: width skipped - type string is ignored in gen 

  // NOTE: height skipped - type string is ignored in gen 

  // NOTE: itemsClassName skipped - type string is ignored in gen 

  // NOTE: outline skipped - type boolean is ignored in gen 

  // NOTE: outlineClassName skipped - type string is ignored in gen 

  // NOTE: icon skipped - type string is ignored in gen 

  // NOTE: text skipped - type string is ignored in gen 

  // NOTE: cellDropOffset skipped - type number is ignored in gen 

  // NOTE: dropPointerSize skipped - type number is ignored in gen 

  // NOTE: rowDropOffset skipped - type number is ignored in gen 

  String? type;
    
  // NOTE: iconsURLPrefix skipped - type string is ignored in gen 

  // NOTE: name skipped - type string is ignored in gen 

  // NOTE: orientation skipped - type string is ignored in gen 

  // NOTE: dateFormat skipped - type string is ignored in gen 

  // NOTE: alternativeFormat skipped - type string is ignored in gen 

  // NOTE: decimalPoint skipped - type string is ignored in gen 

  // NOTE: startRow skipped - type number is ignored in gen 

  // NOTE: endRow skipped - type number is ignored in gen 

  // NOTE: startColumn skipped - type number is ignored in gen 

  // NOTE: endColumn skipped - type number is ignored in gen 

  // NOTE: firstRowAsNames skipped - type boolean is ignored in gen 

  // NOTE: switchRowsAndColumns skipped - type boolean is ignored in gen 

  // NOTE: cell skipped - type string is ignored in gen 

  // NOTE: renderTo skipped - type string is ignored in gen 

  // NOTE: html skipped - type string is ignored in gen 

  // NOTE: dataGridClassName skipped - type string is ignored in gen 

  // NOTE: dataGridID skipped - type string is ignored in gen 

  // NOTE: chartClassName skipped - type string is ignored in gen 

  // NOTE: chartID skipped - type string is ignored in gen 

  // NOTE: allowConnectorUpdate skipped - type boolean is ignored in gen 

  // NOTE: chartConstructor skipped - type string is ignored in gen 

  // NOTE: columnName skipped - type string is ignored in gen 

  // NOTE: value skipped - type string is ignored in gen 

  // NOTE: minFontSize skipped - type number is ignored in gen 

  // NOTE: valueFormat skipped - type string is ignored in gen 

  // NOTE: csv skipped - type string is ignored in gen 

  // NOTE: itemDelimiter skipped - type string is ignored in gen 

  // NOTE: lineDelimiter skipped - type string is ignored in gen 

  // NOTE: useLocalDecimalPoint skipped - type boolean is ignored in gen 

  // NOTE: usePresentationOrder skipped - type boolean is ignored in gen 

  // NOTE: exportIDColumn skipped - type boolean is ignored in gen 

  // NOTE: tableCaption skipped - type string is ignored in gen 

  // NOTE: useMultiLevelHeaders skipped - type boolean is ignored in gen 

  // NOTE: useRowspanHeaders skipped - type boolean is ignored in gen 

  // NOTE: bubblePadding skipped - type number is ignored in gen 

  // NOTE: dragBetweenSeries skipped - type boolean is ignored in gen 

  bool? enableSimulation;
    
  double? friction;
    
  double? gravitationalConstant;
    
  double? initialPositionRadius;
    
  double? maxIterations;
    
  double? maxSpeed;
    
  // NOTE: parentNodeLimit skipped - type boolean is ignored in gen 

  // NOTE: seriesInteraction skipped - type boolean is ignored in gen 

  // NOTE: splitSeries skipped - type boolean is ignored in gen 

  String? approximation;
    
  // NOTE: attractiveForce skipped - type Function is ignored in gen 

  // NOTE: initialPositions skipped - type Function is ignored in gen 

  String? integration;
    
  double? linkLength;
    
  // NOTE: repulsiveForce skipped - type Function is ignored in gen 

  double? theta;
    

  //////////////////////////////////////////////////////////////////////////////
  
  @override
  void toJSONInner(StringBuffer buffer) {
    super.toJSONInner(buffer);

    
    // NOTE: skip serialization of mapNavigation (type MapNavigationOptions is ignored) ignore type: true

    // NOTE: skip serialization of mapView (type MapViewOptions is ignored) ignore type: true

    // NOTE: skip serialization of chart (type ChartOptions is ignored) ignore type: true

    // NOTE: skip serialization of legend (type LegendOptions is ignored) ignore type: true

    // NOTE: skip serialization of tooltip (type TooltipOptions is ignored) ignore type: true

    // NOTE: skip serialization of dataClassColor (type string is ignored) ignore type: true

    // NOTE: skip serialization of dataClasses (type DataClassOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of maxColor (type string is ignored) ignore type: true

    // NOTE: skip serialization of minColor (type string is ignored) ignore type: true

    // NOTE: skip serialization of stops (type GradientColorStop[] is ignored) ignore type: true

    // NOTE: skip serialization of colorAxis (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of dataClasses (type DataClassOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of layout (type string is ignored) ignore type: true

    // NOTE: skip serialization of legend (type LegendOptions is ignored) ignore type: true

    // NOTE: skip serialization of marker (type MarkerOptions is ignored) ignore type: true

    // NOTE: skip serialization of showInLegend (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of borderColor (type string is ignored) ignore type: true

    // NOTE: skip serialization of borderWidth (type number is ignored) ignore type: true

    // NOTE: skip serialization of className (type string is ignored) ignore type: true

    // NOTE: skip serialization of color (type string is ignored) ignore type: true

    // NOTE: skip serialization of connectorClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of connectorColor (type string is ignored) ignore type: true

    // NOTE: skip serialization of connectorDistance (type number is ignored) ignore type: true

    // NOTE: skip serialization of connectorWidth (type number is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of labels (type LabelsOptions is ignored) ignore type: true

    // NOTE: skip serialization of legendIndex (type number is ignored) ignore type: true

    // NOTE: skip serialization of maxSize (type number is ignored) ignore type: true

    // NOTE: skip serialization of minSize (type number is ignored) ignore type: true

    // NOTE: skip serialization of placed (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of ranges (type RangesOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of seriesIndex (type number is ignored) ignore type: true

    // NOTE: skip serialization of sizeBy (type string is ignored) ignore type: true

    // NOTE: skip serialization of sizeByAbsoluteValue (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of zIndex (type number is ignored) ignore type: true

    // NOTE: skip serialization of zThreshold (type number is ignored) ignore type: true

    // NOTE: skip serialization of rangeSelector (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of colors (type string[] is ignored) ignore type: true

    // NOTE: skip serialization of lang (type LangOptions is ignored) ignore type: true

    // NOTE: skip serialization of loading (type LoadingOptions is ignored) ignore type: true

    // NOTE: skip serialization of plotOptions (type SeriesTypePlotOptions is ignored) ignore type: true

    // NOTE: skip serialization of symbols (type (keyof SymbolTypeRegistry)[] is ignored) ignore type: true

    // NOTE: skip serialization of global (type GlobalOptions is ignored) ignore type: true

    // NOTE: skip serialization of xAxis (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of yAxis (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of chart (type ChartOptions is ignored) ignore type: true

    // NOTE: skip serialization of caption (type CaptionOptions is ignored) ignore type: true

    // NOTE: skip serialization of credits (type CreditsOptions is ignored) ignore type: true

    // NOTE: skip serialization of subtitle (type SubtitleOptions is ignored) ignore type: true

    // NOTE: skip serialization of series (type SeriesOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of title (type TitleOptions is ignored) ignore type: true

    // NOTE: skip serialization of time (type TimeOptions is ignored) ignore type: true

    // NOTE: skip serialization of keyCodeMap (type [number[], Function][] is ignored) ignore type: true

    // NOTE: skip serialization of init (type Function is ignored) ignore type: 1

    // NOTE: skip serialization of terminate (type Function is ignored) ignore type: 1

    // NOTE: skip serialization of validate (type Function is ignored) ignore type: 1

    // NOTE: skip serialization of accessibility (type AccessibilityOptions is ignored) ignore type: true

    // NOTE: skip serialization of exporting (type ExportingOptions is ignored) ignore type: true

    // NOTE: skip serialization of navigation (type NavigationOptions is ignored) ignore type: true

    // NOTE: skip serialization of defs (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of annotations (type AnnotationOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of navigator (type NavigatorOptions is ignored) ignore type: true

    // NOTE: skip serialization of scrollbar (type ScrollbarOptions is ignored) ignore type: true

    // NOTE: skip serialization of exposeElementToA11y (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of pane (type PaneOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of connectors (type ConnectorsOptions is ignored) ignore type: true

    // NOTE: skip serialization of zAxis (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of alpha (type number is ignored) ignore type: true

    // NOTE: skip serialization of axisLabelPosition (type "auto" is ignored) ignore type: true

    // NOTE: skip serialization of beta (type number is ignored) ignore type: true

    // NOTE: skip serialization of depth (type number is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of fitToPlot (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of frame (type FrameOptions is ignored) ignore type: true

    // NOTE: skip serialization of viewDistance (type number is ignored) ignore type: true

    // NOTE: skip serialization of isGantt (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of isStock (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of isResponsiveOptions (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of responsive (type Options is ignored) ignore type: true

    // NOTE: skip serialization of rules (type RuleOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of seriesOptions (type SeriesOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of id (type string is ignored) ignore type: true

    // NOTE: skip serialization of parentContainerId (type string is ignored) ignore type: true

    // NOTE: skip serialization of cells (type Options[] is ignored) ignore type: true

    // NOTE: skip serialization of style (type CSSJSONObject is ignored) ignore type: true

    // NOTE: skip serialization of cellsJSON (type JSON[] is ignored) ignore type: true

    // NOTE: skip serialization of id (type string is ignored) ignore type: true

    // NOTE: skip serialization of parentContainerId (type string is ignored) ignore type: true

    // NOTE: skip serialization of copyId (type string is ignored) ignore type: true

    // NOTE: skip serialization of layoutClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of rowClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of cellClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of rows (type Options[] is ignored) ignore type: true

    // NOTE: skip serialization of style (type CSSJSONObject is ignored) ignore type: true

    // NOTE: skip serialization of rowsJSON (type JSON[] is ignored) ignore type: true

    // NOTE: skip serialization of id (type string is ignored) ignore type: true

    // NOTE: skip serialization of width (type string is ignored) ignore type: true

    // NOTE: skip serialization of height (type string is ignored) ignore type: true

    // NOTE: skip serialization of style (type CSSJSONObject is ignored) ignore type: true

    // NOTE: skip serialization of parentContainerId (type string is ignored) ignore type: true

    // NOTE: skip serialization of mountedComponentJSON (type JSON is ignored) ignore type: true

    // NOTE: skip serialization of layout (type Options is ignored) ignore type: true

    // NOTE: skip serialization of layoutJSON (type JSON is ignored) ignore type: true

    // NOTE: skip serialization of responsive (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of className (type string is ignored) ignore type: true

    // NOTE: skip serialization of items (type Options)[] is ignored) ignore type: true

    // NOTE: skip serialization of itemsClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of className (type string is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of menu (type Options is ignored) ignore type: true

    // NOTE: skip serialization of outline (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of outlineClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of icon (type string is ignored) ignore type: true

    // NOTE: skip serialization of text (type string is ignored) ignore type: true

    // NOTE: skip serialization of width (type number is ignored) ignore type: true

    // NOTE: skip serialization of cellDropOffset (type number is ignored) ignore type: true

    // NOTE: skip serialization of dropPointerSize (type number is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of rowDropOffset (type number is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    if (this.type != null) {  
      buffer.writeAll(["\"type\":\`",this.type, "\`,"], "");
    }

    // NOTE: skip serialization of snap (type SnapOptions is ignored) ignore type: true

    // NOTE: skip serialization of styles (type ElementStyles is ignored) ignore type: true

    // NOTE: skip serialization of close (type CloseIcon is ignored) ignore type: true

    // NOTE: skip serialization of contextMenu (type Options is ignored) ignore type: true

    // NOTE: skip serialization of confirmationPopup (type Options is ignored) ignore type: true

    // NOTE: skip serialization of enabled (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of dragDrop (type Options is ignored) ignore type: true

    // NOTE: skip serialization of iconsURLPrefix (type string is ignored) ignore type: true

    // NOTE: skip serialization of lang (type LangOptions is ignored) ignore type: true

    // NOTE: skip serialization of resize (type Options is ignored) ignore type: true

    // NOTE: skip serialization of settings (type SettingsOptions is ignored) ignore type: true

    // NOTE: skip serialization of toolbars (type Toolbars is ignored) ignore type: true

    // NOTE: skip serialization of tools (type Tools is ignored) ignore type: true

    // NOTE: skip serialization of name (type string is ignored) ignore type: true

    if (this.type != null) {  
      buffer.writeAll(["\"type\":\`",this.type, "\`,"], "");
    }

    // NOTE: skip serialization of nestedOptions (type NestedOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of propertyPath (type string[] is ignored) ignore type: true

    // NOTE: skip serialization of selectOptions (type SelectOptions[] is ignored) ignore type: true

    // NOTE: skip serialization of components (type string[] is ignored) ignore type: true

    // NOTE: skip serialization of columnNames (type ColumnNamesOptions is ignored) ignore type: true

    // NOTE: skip serialization of data (type Data is ignored) ignore type: true

    // NOTE: skip serialization of orientation (type string is ignored) ignore type: true

    // NOTE: skip serialization of dateFormat (type string is ignored) ignore type: true

    // NOTE: skip serialization of alternativeFormat (type string is ignored) ignore type: true

    // NOTE: skip serialization of decimalPoint (type string is ignored) ignore type: true

    // NOTE: skip serialization of startRow (type number is ignored) ignore type: true

    // NOTE: skip serialization of endRow (type number is ignored) ignore type: true

    // NOTE: skip serialization of startColumn (type number is ignored) ignore type: true

    // NOTE: skip serialization of endColumn (type number is ignored) ignore type: true

    // NOTE: skip serialization of firstRowAsNames (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of switchRowsAndColumns (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of cell (type string is ignored) ignore type: true

    // NOTE: skip serialization of renderTo (type string is ignored) ignore type: true

    // NOTE: skip serialization of className (type string is ignored) ignore type: true

    if (this.type != null) {  
      buffer.writeAll(["\"type\":\`",this.type, "\`,"], "");
    }

    // NOTE: skip serialization of navigationBindings (type AnyRecord[] is ignored) ignore type: true

    // NOTE: skip serialization of events (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of editableOptions (type Options[] is ignored) ignore type: true

    // NOTE: skip serialization of editableOptionsBindings (type OptionsBindings is ignored) ignore type: true

    // NOTE: skip serialization of presentationModifier (type DataModifier is ignored) ignore type: true

    // NOTE: skip serialization of sync (type RawOptionsRecord is ignored) ignore type: 1

    // NOTE: skip serialization of connector (type ComponentConnectorOptions is ignored) ignore type: true

    // NOTE: skip serialization of id (type string is ignored) ignore type: true

    // NOTE: skip serialization of title (type TextOptionsType is ignored) ignore type: true

    // NOTE: skip serialization of caption (type TextOptionsType is ignored) ignore type: true

    // NOTE: skip serialization of elements (type Node)[] is ignored) ignore type: true

    // NOTE: skip serialization of html (type string is ignored) ignore type: true

    // NOTE: skip serialization of type (type "HTML" is ignored) ignore type: true

    // NOTE: skip serialization of dataPool (type DataPoolOptions is ignored) ignore type: true

    // NOTE: skip serialization of gui (type GUIOptions is ignored) ignore type: true

    // NOTE: skip serialization of editMode (type Options is ignored) ignore type: true

    // NOTE: skip serialization of components (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of componentOptions (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of layoutsJSON (type JSON[] is ignored) ignore type: true

    // NOTE: skip serialization of responsiveBreakpoints (type ResponsiveBreakpoints is ignored) ignore type: true

    // NOTE: skip serialization of dataGridClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of dataGridID (type string is ignored) ignore type: true

    // NOTE: skip serialization of type (type "DataGrid" is ignored) ignore type: true

    // NOTE: skip serialization of dataGridOptions (type DataGridOptions is ignored) ignore type: true

    // NOTE: skip serialization of chartClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of chartID (type string is ignored) ignore type: true

    // NOTE: skip serialization of tableAxisMap (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of sync (type SyncOptions is ignored) ignore type: true

    // NOTE: skip serialization of syncHandlers (type OptionsRecord is ignored) ignore type: 1

    // NOTE: skip serialization of visibleColumns (type string[] is ignored) ignore type: true

    // NOTE: skip serialization of allowConnectorUpdate (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of chartConstructor (type string is ignored) ignore type: true

    // NOTE: skip serialization of connector (type ConnectorOptions is ignored) ignore type: true

    // NOTE: skip serialization of type (type "Highcharts" is ignored) ignore type: true

    // NOTE: skip serialization of chartOptions (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of chartClassName (type string is ignored) ignore type: true

    // NOTE: skip serialization of chartID (type string is ignored) ignore type: true

    // NOTE: skip serialization of columnAssignment (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of sync (type SyncOptions is ignored) ignore type: true

    // NOTE: skip serialization of syncHandlers (type OptionsRecord is ignored) ignore type: 1

    // NOTE: skip serialization of columnName (type string is ignored) ignore type: true

    // NOTE: skip serialization of chartOptions (type Options is ignored) ignore type: true

    // NOTE: skip serialization of style (type CSSObject is ignored) ignore type: true

    // NOTE: skip serialization of threshold (type number[] is ignored) ignore type: true

    // NOTE: skip serialization of thresholdColors (type string[] is ignored) ignore type: true

    // NOTE: skip serialization of type (type "KPI" is ignored) ignore type: true

    // NOTE: skip serialization of value (type string is ignored) ignore type: true

    // NOTE: skip serialization of minFontSize (type number is ignored) ignore type: true

    // NOTE: skip serialization of subtitle (type SubtitleOptions is ignored) ignore type: true

    // NOTE: skip serialization of syncHandlers (type OptionsRecord is ignored) ignore type: 1

    // NOTE: skip serialization of valueFormat (type string is ignored) ignore type: true

    // NOTE: skip serialization of linkedValueTo (type LinkedValueToOptions is ignored) ignore type: true

    // NOTE: skip serialization of sync (type SyncOptions is ignored) ignore type: true

    // NOTE: skip serialization of chartOptions (type Options is ignored) ignore type: true

    // NOTE: skip serialization of columnAssignments (type Generic is ignored) ignore type: true

    // NOTE: skip serialization of sync (type SyncOptions is ignored) ignore type: true

    // NOTE: skip serialization of type (type "Navigator" is ignored) ignore type: true

    // NOTE: skip serialization of csv (type string is ignored) ignore type: true

    // NOTE: skip serialization of decimalPoint (type string is ignored) ignore type: true

    // NOTE: skip serialization of itemDelimiter (type string is ignored) ignore type: true

    // NOTE: skip serialization of lineDelimiter (type string is ignored) ignore type: true

    // NOTE: skip serialization of useLocalDecimalPoint (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of usePresentationOrder (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of json (type GoogleSpreadsheetJSON is ignored) ignore type: true

    // NOTE: skip serialization of decimalPoint (type string is ignored) ignore type: true

    // NOTE: skip serialization of exportIDColumn (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of tableCaption (type string is ignored) ignore type: true

    // NOTE: skip serialization of tableElement (type HTMLElement is ignored) ignore type: true

    // NOTE: skip serialization of useLocalDecimalPoint (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of useMultiLevelHeaders (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of useRowspanHeaders (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of usePresentationOrder (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of labelOptions (type LabelOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of typeOptions (type TypeOptions is ignored) ignore type: true

    // NOTE: skip serialization of boost (type BoostOptions is ignored) ignore type: true

    // NOTE: skip serialization of data (type DataOptions is ignored) ignore type: true

    // NOTE: skip serialization of drilldown (type DrilldownOptions is ignored) ignore type: true

    // NOTE: skip serialization of noData (type NoDataOptions is ignored) ignore type: true

    // NOTE: skip serialization of sonification (type ChartSonificationOptions is ignored) ignore type: 1

    // NOTE: skip serialization of bubblePadding (type number is ignored) ignore type: true

    // NOTE: skip serialization of dragBetweenSeries (type boolean is ignored) ignore type: true

    if (this.enableSimulation != null) {  
      buffer.writeAll(["\"enableSimulation\":",this.enableSimulation, ","], "");
    }

    if (this.friction != null) {  
      buffer.writeAll(["\"friction\":",this.friction, ","], "");
    }

    if (this.gravitationalConstant != null) {  
      buffer.writeAll(["\"gravitationalConstant\":",this.gravitationalConstant, ","], "");
    }

    if (this.initialPositionRadius != null) {  
      buffer.writeAll(["\"initialPositionRadius\":",this.initialPositionRadius, ","], "");
    }

    // NOTE: skip serialization of marker (type BubblePointMarkerOptions is ignored) ignore type: true

    if (this.maxIterations != null) {  
      buffer.writeAll(["\"maxIterations\":",this.maxIterations, ","], "");
    }

    if (this.maxSpeed != null) {  
      buffer.writeAll(["\"maxSpeed\":",this.maxSpeed, ","], "");
    }

    // NOTE: skip serialization of parentNodeLimit (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of parentNodeOptions (type Options is ignored) ignore type: true

    // NOTE: skip serialization of seriesInteraction (type boolean is ignored) ignore type: true

    // NOTE: skip serialization of splitSeries (type boolean is ignored) ignore type: true

    if (this.approximation != null) {  
      buffer.writeAll(["\"approximation\":\`",this.approximation, "\`,"], "");
    }

    // NOTE: skip serialization of attractiveForce (type Function is ignored) ignore type: 1

    if (this.enableSimulation != null) {  
      buffer.writeAll(["\"enableSimulation\":",this.enableSimulation, ","], "");
    }

    if (this.friction != null) {  
      buffer.writeAll(["\"friction\":",this.friction, ","], "");
    }

    if (this.gravitationalConstant != null) {  
      buffer.writeAll(["\"gravitationalConstant\":",this.gravitationalConstant, ","], "");
    }

    if (this.initialPositionRadius != null) {  
      buffer.writeAll(["\"initialPositionRadius\":",this.initialPositionRadius, ","], "");
    }

    // NOTE: skip serialization of initialPositions (type Function is ignored) ignore type: 1

    if (this.integration != null) {  
      buffer.writeAll(["\"integration\":\`",this.integration, "\`,"], "");
    }

    if (this.linkLength != null) {  
      buffer.writeAll(["\"linkLength\":",this.linkLength, ","], "");
    }

    if (this.maxIterations != null) {  
      buffer.writeAll(["\"maxIterations\":",this.maxIterations, ","], "");
    }

    if (this.maxSpeed != null) {  
      buffer.writeAll(["\"maxSpeed\":",this.maxSpeed, ","], "");
    }

    // NOTE: skip serialization of repulsiveForce (type Function is ignored) ignore type: 1

    if (this.theta != null) {  
      buffer.writeAll(["\"theta\":",this.theta, ","], "");
    }

    if (this.type != null) {  
      buffer.writeAll(["\"type\":\`",this.type, "\`,"], "");
    }

    // NOTE: skip serialization of stockTools (type StockToolsOptions is ignored) ignore type: true
  }

}
