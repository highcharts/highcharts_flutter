name: highcharts_flutter_integration_test
description: Highcharts Flutter test integration
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  highcharts_flutter:
    path: ../
  highcharts_flutter_webwebview: ^0.3.4

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true


